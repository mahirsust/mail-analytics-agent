"""MCP client wrapper for Microsoft's published M365 MCP server.

Handles connection lifecycle and passes the user's OBO token so the
server can call Graph on behalf of the authenticated user.
"""

# TODO: Piece 6 — implement M365 MCP client with OBO token injection
