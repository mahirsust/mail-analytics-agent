"""Entra ID On-Behalf-Of (OBO) token exchange.

Validates the caller's JWT and exchanges it for a Graph-scoped token
so the agent acts as the user, not a service account.
"""

# TODO: Piece 6 — implement OBO flow with MSAL
