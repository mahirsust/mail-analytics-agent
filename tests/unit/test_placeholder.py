# Placeholder — keeps CI green while unit tests are being written (Piece 3+).
# Delete this file once real unit tests exist.


def test_placeholder() -> None:
    """Stub test so pytest does not exit with code 5 (no tests collected)."""
    assert True
